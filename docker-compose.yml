version: '3'
services:
  frontend:
    build: .
    ports:
      - "80:80"
    depends_on:
      - backend
  backend:
    build: ../backend  # Adjust this path to your backend project location
    ports:
      - "8080:8080"